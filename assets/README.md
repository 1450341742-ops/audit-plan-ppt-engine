# assets

V8.0 已支持 Streamlit Cloud / Windows / Mac 通用运行，不再依赖 pywin32 或 Microsoft PowerPoint COM。

可选：如果需要套用本地模板，请将模板上传到本目录，并命名为：

```text
assets/template.pptx
```

说明：没有 template.pptx 时，系统会自动生成标准蓝色商务风 PPT，不会再出现 `No module named win32com` 报错。
